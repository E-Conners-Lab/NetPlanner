# NetPlanner backend dependencies — pinned to exact versions (SEC-30).
# Regenerate / bump deliberately; review changelogs before upgrading.

# --- Web framework ---
fastapi==0.136.1
uvicorn[standard]==0.47.0

# --- Database (async SQLite) ---
SQLAlchemy==2.0.49
aiosqlite==0.22.1
alembic==1.18.4
greenlet==3.5.0

# --- Validation / settings ---
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# --- AI layer (Phase 2+) ---
anthropic==0.103.1

# --- PDF generation (Phase 5) ---
weasyprint==68.1
markdown==3.10.2

# --- Testing ---
pytest==9.0.3
pytest-asyncio==1.3.0
httpx==0.28.1
