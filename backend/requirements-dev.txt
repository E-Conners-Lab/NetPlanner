# NetPlanner backend DEV dependencies — linting, formatting, type checking,
# security scanning, and coverage. Pinned to exact versions (SEC-30).
#
# Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

ruff==0.15.13
black==26.5.1
isort==8.0.1
mypy==2.1.0
bandit==1.9.4
pytest-cov==7.1.0
