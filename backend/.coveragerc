# Coverage config for the backend.
# SQLAlchemy's async engine bridges sync/async via greenlet; coverage must be
# told to track greenlet concurrency or it loses tracing through DB calls.
[run]
source = app
concurrency = greenlet,thread

[report]
show_missing = True
